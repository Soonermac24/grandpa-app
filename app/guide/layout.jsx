export const metadata = {
  title: 'How to use Papa App',
  description: 'A simple step-by-step guide to Papa App',
  manifest: '/manifest.json',
}

export default function GuideLayout({ children }) {
  return children
}
